amqp:
  exchange:
    greetings:
      topic: greetings.topic
      routing:
        vowels : greetings.vowels
        consonants : greetings.consonants
  queue: vowels.queue

logging:
  level:
    org:
      springframework:
        amqp:
          rabbit:
            core:
              RabbitAdmin: DEBUG